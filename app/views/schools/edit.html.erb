<!-- Validation failure messages -->
<% if @school.errors.any? %>
  <% @school.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit School #<%= @school.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_school/<%= @school.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for school_name -->
      <div class="form-group">
        <label for="school_name" class="control-label">
          School name
        </label>

        <input type="text" id="school_name" name="school_name" placeholder="School name" class="form-control" value="<%= @school.school_name %>">
      </div>

      <!-- Label and input for total_enrollment -->
      <div class="form-group">
        <label for="total_enrollment" class="control-label">
          Total enrollment
        </label>

        <input type="text" id="total_enrollment" name="total_enrollment" placeholder="Total enrollment" class="form-control" value="<%= @school.total_enrollment %>">
      </div>

      <!-- Label and input for state -->
      <div class="form-group">
        <label for="state" class="control-label">
          State
        </label>

        <input type="text" id="state" name="state" placeholder="State" class="form-control" value="<%= @school.state %>">
      </div>

      <button class="btn btn-warning">
        Update School
      </button>
      or
      <a href="/schools/<%= @school.id %>">Cancel</a>
    </form>
  </div>
</div>
