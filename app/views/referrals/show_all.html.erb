<a href="/" class="btn btn-lg btn-warning">Back to My Dashboard</a>

<div class="page-header">
  <h1>
    My Referrals - Details
  </h1>
  <p><em><small>
    If no email address is present, the user was not invited through the TransparentCareer portal, but signed up by clicking on your unique referral link
  </small></em></p>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Email</th>
        <th>Accepted?</th>
      </tr>

      <% @referrals.each do |referral| %>
      <tr>
        <td><a href = "mailto:<%= referral.invited_user_email %>"><%= referral.invited_user_email %></a></td>
        <td><%= referral.accepted_at.year.to_s+"-"+referral.accepted_at.month.to_s+"-"+referral.accepted_at.day.to_s %></td>
      </tr>
      <% end %>

    </table>
  </div>
</div>
